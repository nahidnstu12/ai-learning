# api-server

Add when you want HTTP (`POST /chat`) for a UI or external clients — usually after `chat-cli` patterns are stable.

Suggested stack later: Fastify or Express + same Ollama calls as `apps/chat-cli`.
