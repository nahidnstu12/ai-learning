# 03 — RAG & chunking

## Goal

- [ ] Load docs from `data/raw/`, chunk, embed, retrieve top-k.
- [ ] Answer only from retrieved context; log which chunks were used.

## Run

(TBD)

## Done when

Changing chunk size visibly changes retrieval quality (note in `docs/notes.md`).
