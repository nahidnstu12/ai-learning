# Copy to .env (repo root .gitignore already ignores .env)
DATABASE_URL=postgresql://admin:password@localhost:5433/ai_learning

# Ollama embeddings (same as experiments/04-embeddings-similarity)
OLLAMA_URL=http://localhost:11434
OLLAMA_EMBED_MODEL=nomic-embed-text
